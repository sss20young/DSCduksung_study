import React from 'react';
import LoginInsert from './LoginInsert';

const LoginTemplate = () => {
    return (
        <div>
            <h2>로그인</h2>
            <LoginInsert />
        </div>
    );
};

export default LoginTemplate;